import React from 'react';

const SectionTitle = ({ text }) => {
    return (
        <div className='borddr-b border-base-300 pb-5'>

            <h2 className='text-2xl font-bold tracking-normal'>{text}</h2>

        </div>
    );
}

export default SectionTitle;
